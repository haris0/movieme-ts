import { NextPage } from 'next';
import { Container } from 'react-bootstrap';

const Show: NextPage = () => (
  <Container className="container-custome">
    <h1>Tv Show Page</h1>
  </Container>
);

export default Show;
